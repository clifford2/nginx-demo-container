# SPDX-FileCopyrightText: © 2026 Clifford Weinmann <https://www.cliffordweinmann.com/>
# SPDX-License-Identifier: MIT-0
#
# Expose the nginx-demo app as a ClusterIP service

---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "nginx-demo"
  labels:
    app.kubernetes.io/name: "nginx-demo"
    app.kubernetes.io/instance: "nginx-demo"
    app.kubernetes.io/component: "website"
    app: "nginx-demo"
spec:
  type: "ClusterIP"
  ports:
    - name: "http"
      port: 8080
      protocol: "TCP"
      targetPort: "http"
  selector:
    app.kubernetes.io/name: "nginx-demo"
  sessionAffinity: "None"
