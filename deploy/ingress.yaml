# SPDX-FileCopyrightText: © 2026 Clifford Weinmann <https://www.cliffordweinmann.com/>
# SPDX-License-Identifier: MIT-0

---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "nginx-demo"
spec:
  ingressClassName: "nginx"
  rules:
    - host: "nginx-demo.example.net"
      http:
        paths:
          - backend:
              service:
                name: "nginx-demo"
                port:
                  number: 8080
            path: "/"
            pathType: "Prefix"
